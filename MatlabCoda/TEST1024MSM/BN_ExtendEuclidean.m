function [x] = BN_ExtendEuclidean( a , p)
%欧几里得扩展定理
%  ====================================================
% Function：计算x，其中ax=1(modp),即ax-yp=1  
% Input Para：a-椭圆曲线上的坐标，二进制低位在前，x（0）；
% p-大素数，二进制低位在前，p（0）；
% Output：x -ax-yp=1  。
% Author by Wang Pengfei
% data：2022.3.13
% Revised by：
% ====================================================








end
